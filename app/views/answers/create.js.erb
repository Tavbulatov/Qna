$('.flash_message').html('<%= render 'shared/flash'%>');
$('.answer_errors').html('<%= render 'shared/errors', resource: @answer%>')

<% if @answer.persisted?%>
  $('form.new-answer #answer_body').val('')
  $('form.new-answer #answer_files').val('')
  $('.link_fields').remove()
<% end%>
